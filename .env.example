# 数据库配置（Supabase）
# 获取方式：Supabase Dashboard → 项目 Settings → API
# 注意：变量名必须是 COZE_ 前缀，代码中读取的就是这两个名字

COZE_SUPABASE_URL=your-project-url
COZE_SUPABASE_ANON_KEY=your-anon-key

# 用户登录令牌签名密钥（随机长字符串，可用 openssl rand -base64 32 生成）
JWT_SECRET=your-jwt-secret

# 可选：Supabase service role key（配置后头像上传走服务端权限，更安全）
# Supabase Dashboard → Settings → API → service_role key
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
